<Window x:Class="AbhimoTracker.Employee.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Abhimo Tracker -- Sign in" Height="320" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sign in to Abhimo Tracker" FontSize="16" FontWeight="Bold" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="1" Text="Username"/>
        <TextBox Grid.Row="2" x:Name="UsernameBox" Margin="0,4,0,10"/>

        <TextBlock Grid.Row="3" Text="Password"/>
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" Margin="0,4,0,10"/>

        <TextBlock Grid.Row="5" x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button x:Name="SignInButton" Content="Sign in" Width="80" IsDefault="True" Click="SignInButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
