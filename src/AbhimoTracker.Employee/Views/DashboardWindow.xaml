<Window x:Class="AbhimoTracker.Employee.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Abhimo Tracker" Height="360" Width="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="UsernameText" Text="Signed in as ..." FontSize="18" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" x:Name="StatusText" Text="Status: loading" Margin="0,6,0,0" Foreground="Gray"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0">
            <Button x:Name="SignInButton" Content="Sign in" Width="110" Height="36" Margin="0,0,10,0" Click="SignInButton_Click"/>
            <Button x:Name="SignOutButton" Content="Sign out" Width="110" Height="36" Click="SignOutButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="SessionText" Margin="0,16,0,0" Text=""/>

        <TextBlock Grid.Row="5" x:Name="MessageText" Foreground="DarkRed" TextWrapping="Wrap"/>
    </Grid>
</Window>
